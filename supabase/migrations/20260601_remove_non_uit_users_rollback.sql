-- WARNING: This rollback CANNOT restore the deleted rows. Once removed,
-- auth.users entries are gone along with their cascaded profile/course data.
-- This file exists only to satisfy the project's "every migration ships
-- with a rollback" rule; it intentionally performs no action.

SELECT 1 WHERE FALSE;
